#include <iostream> // permite que o programa gere saída de dados na tela

// função que inicia a execução do programa
int main()
{
    std::cout << "Welcome to C++!\n"; // exibe a mensagem;
    /*quando a instrução precedente é executada, ela envia o fluxo de caracteres Welcome to C++!\n para o
    objeto de fluxo de saída padrão — std::cout — que, normalmente, é ‘conectado’ à tela*/

    return 0; // indica que o programa encerrou com sucesso

}// fim da função main
