#include <iostream>

using std::cout;
using std::cin;
using std::endl;

int main()
{

    int sum, x;

    //defining variable settings
    x = 1;
    sum = 0;

    sum += x;   //or sum = sum + x

    cout << "The sum is " << sum << endl;

    return 0;
}
